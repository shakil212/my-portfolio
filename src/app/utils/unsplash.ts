// Placeholder for Unsplash utility
// This would be replaced with actual Unsplash integration if needed
export const unsplash_tool = (query: string) => {
  return `https://source.unsplash.com/800x600/?${encodeURIComponent(query)}`;
};
